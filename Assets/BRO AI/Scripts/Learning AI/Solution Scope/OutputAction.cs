namespace BRO.AI.Learning
{
    /// <summary>
    /// This enum keeps track on all available output functions.
    /// </summary>
    public enum OutputAction
    {
        DoNothing,
        DodgeBeast,
        GainDistance,
        PassBallRandomly
    }
}